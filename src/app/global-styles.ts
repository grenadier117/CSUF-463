import { createTheme } from '@mui/material';

export const theme = createTheme({
  spacing: 6,
  palette: {
    primary: {
      main: '#1a1a1a',
    },
    secondary: {
      main: '#595959',
    },
    background: {
      paper: '#404040',
      default: '#FF0000',
    },
  },
});
